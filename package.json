{
  "name": "blood-donate-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "react": "cd React && npm run react",
    "dev": "concurrently \"npm run react\" \"npm run server\"",
    "heroku-postbuild": "cd React && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^3.4.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.7.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "nodemon": "^1.19.1"
  }
}